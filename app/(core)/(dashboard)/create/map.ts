const baseUrl = `/create`;

export const createMap = {
  home: {
    spaces: {
      link: `${baseUrl}/spaces`,
    },
    flow: {
      link: `${baseUrl}/flow`,
    },
    link: `${baseUrl}`,
  },
};
