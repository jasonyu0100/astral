const baseUrl = `/feed`;

export const updatesMap = {
  updates: {
    following: {
      link: `${baseUrl}/following`,
    },
    explore: {
      link: `${baseUrl}/explore`,
    },
    link: `${baseUrl}`,
  },
};
