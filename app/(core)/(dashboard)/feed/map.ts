const baseUrl = `/feed`;

export const feedsMap = {
  feed: {
    following: {
      link: `${baseUrl}/following`,
    },
    explore: {
      link: `${baseUrl}/explore`,
    },
    link: `${baseUrl}`,
  },
};
