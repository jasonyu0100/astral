const baseUrl = `/network`;

export const networkMap = {
  network: {
    creative: {
      link: `${baseUrl}/creative`,
    },
    inbox: {
      link: `${baseUrl}/inbox`,
    },
    link: `${baseUrl}`,
  },
};
