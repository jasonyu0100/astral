export interface StickyNoteProps {
    multiple ?: boolean;
}

export default function FlowStickyNote({ multiple } : StickyNoteProps) {
    return <div className="w-[100px] h-[100px] bg-yellow-200 flex-shrink-0">

    </div>
}