import React from "react";

export default function DriveSection({ children} : {children: React.ReactNode}) {
    return <div className="flex flex-col space-y-[2rem]">
        {children}
            </div>
}