export function GalleryFolderInfo({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex flex-col space-y-[1rem] flex-grow h-full p-[1rem] items-center justify-center">
      {children}
    </div>
  );
}
