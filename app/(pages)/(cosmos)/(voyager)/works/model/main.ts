import { exampleWork, exampleWorks } from "./work/example";

export const worksModel = {
  works: {
    work: {
      example: exampleWork,
      reducers: []
    },
    example: exampleWorks,
  },
};
