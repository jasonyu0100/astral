const baseUrl = `/spaces`

export const spacesMap = {
  spaces: {
    now: {
      link: `${baseUrl}/now`,
    },
    then: {
      link: `${baseUrl}/then`,
    },
    all: {
      link: `${baseUrl}/all`,
    },
    link: `${baseUrl}`,
  },
};
