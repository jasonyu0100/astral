import { exampleBoard, exampleBoards } from "./main";
import { examplePins, examplePin } from "./pin/main";

export const boardTable = {
  example: exampleBoard,
  examples: exampleBoards,
  pin: {
    example: examplePin,
    examples: examplePins,
  },
};
